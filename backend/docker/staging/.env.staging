# =============================================================================
# Environment Variables - Staging
# =============================================================================
# Copy this file and customize values for your staging environment
# Usage: docker-compose --env-file .env.staging -f docker-compose-staging.yml up -d
# =============================================================================

# PostgreSQL Configuration
POSTGRES_DB=garage_manager_staging
POSTGRES_USER=gm_user_staging
POSTGRES_PASSWORD=admin

# JWT Configuration
JWT_SECRET=a9c3f7e2b1d4f6a8c0e7b3d5f9a2c6e1
JWT_EXPIRATION=900000
JWT_RENEWAL_THRESHOLD=300000


# =============================================================================
# Environment Variables - Production
# =============================================================================
# Copy this file and customize values for your production environment
# Usage: docker-compose --env-file .env.prod -f docker-compose.yml up -d
# =============================================================================

# PostgreSQL Configuration
POSTGRES_DB=garage_manager_prod
POSTGRES_USER=gm_user_prod
POSTGRES_PASSWORD=admin

# JWT Configuration
JWT_SECRET=4f8d2e9a7b1c6d5f3e0a9b2c7d8e1f0a
JWT_EXPIRATION=900000
JWT_RENEWAL_THRESHOLD=300000


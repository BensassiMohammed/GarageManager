# =============================================================================
# Environment Variables - Production
# =============================================================================
# Usage: docker-compose --env-file .env.prod -f docker-compose.yml up -d
# =============================================================================
# IMPORTANT: Change all default passwords before deploying to production!
# =============================================================================

# Database
POSTGRES_DB=garage_manager_prod
POSTGRES_USER=gm_user_prod
POSTGRES_PASSWORD=admin

# Backend
SPRING_PROFILES_ACTIVE=prod

# Frontend
ANGULAR_PROFILE=production

# JWT Configuration (MUST change in production - minimum 32 characters)
JWT_SECRET=a9c3f7e2b1d4f6a8c0e7b3d5f9a2c6e1
